name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: make build

      - name: Test
        run: make test

      - name: Bundle
        run: make bundle

      - name: Verify app structure
        run: |
          test -f "ClawdBar.app/Contents/MacOS/clawdbar"
          test -f "ClawdBar.app/Contents/Info.plist"
